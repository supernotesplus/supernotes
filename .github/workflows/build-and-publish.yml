name: Generate, Build, and Publish Content

on:
  push:
    branches:
      - main
  # 💡 الجدولة: تشغيل تلقائي يومي في الساعة 3:00 صباحاً بتوقيت UTC
  # (يمكنك تغيير هذه الساعة بتعديل قيمة cron)
  schedule:
    - cron: '0 3 * * *'
  # يسمح بالتشغيل اليدوي من واجهة GitHub Actions
  workflow_dispatch:

jobs:
  generate_and_publish:
    runs-on: ubuntu-latest
    steps:
      - name: ⚙️ Checkout Repository
        uses: actions/checkout@v4
        # نستخدم fetch-depth: 0 لضمان جلب سجل Git بالكامل
        with:
          fetch-depth: 0
          
      # ==========================================================
      # 🛠️ تثبيت البيئة الضرورية (Go و Python)
      # ==========================================================
      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.20'
          
      - name: 🐍 Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      # ==========================================================
      # 🚀 تشغيل المحرك (التوليد + الحقن)
      # ==========================================================
      - name: 🤖 Run Generator and Injector Engine (via main.go)
        # سيقوم main.go بتشغيل generator.py (التوليد) ثم inject_affiliates.py (الحقن)
        run: go run main.go
          
      # ==========================================================
      # 💾 تثبيت وإرسال الملفات الجديدة
      # ==========================================================
      - name: 💾 Commit New Files (Markdown, CSVs)
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: '🤖 Automated: Generated new content and injected affiliate links.'
          branch: main
          # يشمل ملفات Markdown الجديدة، ملفات CSV المعدلة، وملفات الإعلانات
          file_pattern: 'content/posts/*.md content-engine/*.csv affiliate_lookup.csv data/ads.json' 
          
      # ==========================================================
      # 🏗️ بناء ونشر الموقع (Hugo)
      # ==========================================================
      - name: Setup Hugo
        uses: peaceiris/actions-hugo@v3
        with:
          hugo-version: 'latest'
          extended: true

      - name: 🏗️ Hugo Build
        run: hugo --minify
        
      # 🚨 مهم: يجب أن تقوم بتفعيل GitHub Pages في إعدادات المستودع
      - name: 🚀 Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./public
          # نشر إلى فرع gh-pages أو فرع النشر الخاص بك (إذا كان مختلفاً)
          # publish_branch: gh-pages
